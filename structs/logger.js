/*
const logger = require("winston");

logger.configure({
  level: "info",
  format: logger.format.combine(
    logger.format.timestamp(),
    logger.format.json()
  ),
  transports: [
    new logger.transport.defaultMaxListeners({ filename: "backend.log" }),
  ],
});

module.exports = logger;
*/